# Task 7 Reminders

## next.config.mjs

`next.config.mjs` has a `TODO(task-7)` comment at the top. When Task 7 runs:

1. Install `@payloadcms/next`
2. Uncomment the import: `import { withPayload } from '@payloadcms/next/withPayload';`
3. Change the export back to: `export default withPayload(nextConfig);`
4. Remove the TODO comment block.

Reference: `docs/superpowers/plans/2026-04-17-phase-0-scaffold.md` Task 7.
